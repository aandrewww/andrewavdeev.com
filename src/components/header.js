/** @jsx jsx */
// import PropTypes from 'prop-types';
import { jsx } from 'theme-ui';

const Header = () => {
  return (
    <div sx={{ flexShrink: 0 }}>...Header...</div>
  );
};

export default Header;
